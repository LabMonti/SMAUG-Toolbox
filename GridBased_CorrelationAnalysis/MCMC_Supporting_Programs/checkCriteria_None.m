function pass = checkCriteria_None(CGO,sequence,params)
    %The null choice when you don't want to add any extra criteria; always
    %returns true, meaning the node-sequence is allowed
    
    pass = true;
end